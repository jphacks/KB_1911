type Room @model
  @auth(rules: [{allow: groups, groups: ["Admin"]}]) {
  id: ID!
  name: String!
  messages: [Message] @connection
}

type Message @model
  @auth(rules: [{allow: owner}]) {
  id: ID!
  content: String
  owner: String
  mention: String
}
